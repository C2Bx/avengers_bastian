{% extends 'base.html.twig' %}

{% block title %}Modifier {% if auteur is defined %}l'auteur {{ auteur.nom }}{% else %}le livre {{ livre.titre }}{% endif %}{% endblock %}

{% block body %}
    <h1>Modifier {% if auteur is defined %}l'auteur {{ auteur.nom }}{% else %}le livre {{ livre.titre }}{% endif %}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
